//! Canvas-to-image compositor (stub).
//!
//! Phase 5 will implement the `render_canvas` function using `tiny-skia` to
//! composite resolved widget trees into `RgbaImage` outputs. This module is a
//! placeholder so the crate compiles with its full module tree.
